<div class="navbar navbar-default navbar-fixed-top" role="navigation" ng-controller="HeaderCtrl">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Freshbooks reporter</a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav" id="header-btns" style="display: none;">
        <li ng-class="{active: isActive('/dashboard')}" ng-show="isAuthenticated()"><a href="" ng-click="location.path('/dashboard')">Dashboard</a></li>
        <li ng-class="{active: isActive('/settings')}" ng-show="isAuthenticated()"><a href="" ng-click="location.path('/settings')">Settings</a></li>
        <li ng-class="{active: isActive('/adduser')}" ng-show="isAuthenticated() && isAdmin()"><a href="" ng-click="location.path('/adduser')">Add User</a></li>
        <li ng-class="{active: isActive('/datareload')}" ng-show="isAuthenticated() && isAdmin()"><a href="" ng-click="location.path('/datareload')">Data Reload</a></li>
        <li ng-show="isAuthenticated()"><a href="" ng-click="logout()">Logout</a></li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>
